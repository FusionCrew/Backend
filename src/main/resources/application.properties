# Server Configuration
server.port=8080
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Application Name
spring.application.name=aikiosk-backend

# Logging
logging.level.root=INFO
logging.level.com.fusioncrew=DEBUG

# DevTools Configuration - disable auto-restart for stability
spring.devtools.restart.enabled=false

# Database Configuration (Supabase PostgreSQL - Transaction Pooler)
# Using Transaction Pooler (port 6543) for better compatibility with IPv4 environments
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres.kjecwbwohptcskdblgig
spring.datasource.password=bxFN?VrDwxV62.G
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# HikariCP Configuration - Connection pool settings for Supabase PgBouncer stability
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=120000
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=5000

# Security / JWT
jwt.secret=v3ryS3cr3tK3yF0rHmacSh4256MustB3AtL3ast32Byt3sL0ng!!
jwt.expiration=3600000
jwt.refresh-token.expiration=604800000

# AI Server Configuration
ai.server.url=http://localhost:8000

# Supabase Configuration
supabase.url=${SUPABASE_URL}
supabase.publishable-key=${SUPABASE_PUBLISHABLE_KEY}
supabase.anon-key=${SUPABASE_ANON_KEY}
