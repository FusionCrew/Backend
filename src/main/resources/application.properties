# Server Configuration
server.port=8080
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Application Name
spring.application.name=aikiosk-backend

# Allow Bean Definition Overriding (for jpaAuditingHandler duplicate)
spring.main.allow-bean-definition-overriding=true

# Logging
logging.level.root=INFO
logging.level.com.fusioncrew=DEBUG

# Database Configuration (Supabase PostgreSQL - Session Pooler)
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres.rxfdeaizxpybdpffxbxq
spring.datasource.password=Dleodus3380^^
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.default_batch_fetch_size=100

# 백엔드가 계속 2분마다 종료되어서 설정해둠.
spring.datasource.hikari.max-lifetime=0

# Security / JWT
jwt.secret=v3ryS3cr3tK3yF0rHmacSh4256MustB3AtL3ast32Byt3sL0ng!!
jwt.expiration=3600000
jwt.refresh-token.expiration=604800000

# AI Server Configuration
ai.server.url=http://localhost:8000

# Supabase Configuration
supabase.url=${SUPABASE_URL}
supabase.publishable-key=${SUPABASE_PUBLISHABLE_KEY}
supabase.anon-key=${SUPABASE_ANON_KEY}

# Max Request Size (Support large Base64 images)
server.tomcat.max-http-post-size=10MB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

